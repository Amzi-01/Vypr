#!/usr/bin/env bash
# Run a command in the RDPWindows guest over the dedicated build-loop key.
exec ssh -i "$HOME/.ssh/sash-guest" \
    -o BatchMode=yes -o StrictHostKeyChecking=no -o LogLevel=ERROR \
    -o ConnectTimeout=10 Lucy@192.168.122.58 "$@"
